name: debian-bullseye
on: [push, pull_request]

jobs:
  build:
    runs-on: ubuntu-latest
    name: build

    steps:
      - name: git checkout
        uses: actions/checkout@v2
      - name: build
        uses: ./.github/actions/debian-bullseye
